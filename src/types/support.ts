export interface KnowledgebaseArticle {
  slug: string;
  title: string;
  summary: string;
  category: string;
}

export interface SupportTicket {
  subject: string;
  message: string;
  category: string;
}

